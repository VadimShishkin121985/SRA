name: Automated API tests


on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  api-test-job:
    name: Run API Tests
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Prepare test results folder
      run: |
        echo "Создаём папку для отчёта тестов..."
        mkdir -p ./api/test_results/

    - name: Run API Tests
      run: |
        echo "🏁 Запуск API тестов..."
        python api/dt_api.py || { echo "❌ Ошибка в API тестах"; exit 1; }
        echo "✅ API тесты завершены"

    - name: Save test report
      if: always()  # сохраняем даже при падении тестов
      uses: actions/upload-artifact@v4
      with:
        name: api-test-report
        path: ./api/test_results/
        retention-days: 7

  publish-report:
    name: Publish Test Report
    runs-on: ubuntu-latest
    needs: api-test-job

    steps:
    - name: Download test report artifact
      uses: actions/download-artifact@v4
      with:
        name: api-test-report
        path: ./downloaded_report/

    - name: Show test report
      run: |
        echo "📄 Содержимое тестового отчета:"
        cat ./downloaded_report/*.txt || echo "⚠️ Файл отчета не найден"